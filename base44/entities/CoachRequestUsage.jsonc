{
  "name": "CoachRequestUsage",
  "type": "object",
  "description": "Minimal server-only reservations used to enforce shared AI coach quotas. No prompts or health data are stored.",
  "properties": {
    "description": {
      "type": "string",
      "maxLength": 1000
    },
    "owner_id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "description": "Authenticated account identifier set only by the backend function."
    },
    "feature": {
      "type": "string",
      "minLength": 1,
      "maxLength": 48,
      "enum": [
        "coach",
        "meal_plan_ai_variety",
        "body_composition",
        "food_photo"
      ],
      "description": "Paid-inference feature this reservation belongs to, so one feature's quota cannot consume another's."
    },
    "request_id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 64,
      "pattern": "^[A-Za-z0-9-]+$",
      "description": "Opaque reservation identifier; it never contains user content."
    },
    "requested_at": {
      "type": "string",
      "format": "date-time",
      "description": "Server timestamp used for the hourly and daily quota windows."
    }
  },
  "required": [
    "owner_id",
    "feature",
    "request_id",
    "requested_at"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": {
      "user_condition": {
        "role": "admin"
      }
    },
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}