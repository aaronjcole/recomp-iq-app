{
  "name": "CurrentStrategy",
  "type": "object",
  "properties": {
    "calorie_target": {
      "type": "number",
      "minimum": 0,
      "maximum": 20000
    },
    "protein_target_g": {
      "type": "number",
      "minimum": 0,
      "maximum": 2000
    },
    "fat_target_g": {
      "type": "number",
      "minimum": 0,
      "maximum": 2000
    },
    "carb_target_g": {
      "type": "number",
      "minimum": 0,
      "maximum": 3000
    },
    "step_target": {
      "type": "number",
      "minimum": 0,
      "maximum": 200000
    },
    "lifting_days_target": {
      "type": "number",
      "minimum": 0,
      "maximum": 7
    },
    "cardio_days_target": {
      "type": "number",
      "minimum": 0,
      "maximum": 7
    },
    "goal_type": {
      "type": "string",
      "enum": [
        "fat_loss_biased_recomp",
        "body_recomposition",
        "fat_loss",
        "aggressive_fat_loss",
        "muscle_gain",
        "lean_bulk",
        "aggressive_gain",
        "maintenance",
        "strength_retention_cut"
      ]
    },
    "weekly_adjustment_rule": {
      "type": "string"
    },
    "behavior_focus": {
      "type": "string"
    },
    "manual_override": {
      "type": "boolean",
      "default": false,
      "description": "When true, the weekly check-in keeps its adaptive recommendation advisory and does not overwrite these targets."
    }
  },
  "required": [
    "calorie_target",
    "protein_target_g",
    "fat_target_g",
    "carb_target_g",
    "step_target",
    "lifting_days_target",
    "cardio_days_target",
    "goal_type"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}
